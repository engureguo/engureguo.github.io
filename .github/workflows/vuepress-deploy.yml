name: build & deploy pages

on:
  push:
    branches:
    - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3

    - name: Use Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18.x'

    - name: Build Front-end files
      run: |
        yarn
        export NODE_OPTIONS=--openssl-legacy-provider
        yarn docs:build
        cd docs/.vuepress/dist
        git init
        git config --local user.email "engureguo"
        git config --local user.name "57861661+engureguo@users.noreply.github.com"
        git commit -a -m "Add changes"
        git push -f https://${{ secrets.ACCESS_TOKEN }}@github.com/engureguo/engureguo.github.io.git gh-pages
